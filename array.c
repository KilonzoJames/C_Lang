// #include <cs50.h>
// #include <stdio.h>

// int main ()
// {
//     // prompt user for length
//     int length;
//     do
//     {
//         length = get_int("Size: ");
//     }
//     while (length < 0);

//     // declare an array
//     int twice[length];

//     // declare the first value
//     twice[0] = 1;
//     printf("%i\n", twice[0]);

//     for (int i = 1; i < length; i++)
//     {
//         // make the current element twice the previous one
//         twice[i] = 2 * twice[i - 1];
//         // print the current element
//         printf("%i\n", twice[i]);
//     }

// }

#include <stdio.h>

int main(void)
{
    // string first = get_string("What's  is your first name? ");
    // string last = get_string("What's your last name? ");
    // printf("hello, %s %s\n", first, last);
    for (int i = 0; i < 3; i++)
    {
        printf("I got 100%%\n");
    }
}
#include <stdio.h>

int main()
{
    int scores[1024];

    // Initialize the array elements with some values (for example, using a loop)

    // Print the initialized values
    for (int i = 0; i < 1024; i++) {
        // scores[i] = i * 2;  // You can replace this with any initialization logic you want
        printf("%i\n", scores[i]);
    }

    return 0;
}

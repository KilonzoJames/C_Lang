#include <stdio.h>

int main(void) {
    int i = 42;
    i++;

    // Using %d to print the integer
    printf("i is now %d\n", i);

    // Using %i to print the integer
    printf("i is now %i\n", i);
    return 0;
}
